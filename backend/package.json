{
  "name": "devsecops-sample-app",
  "version": "1.0.0",
  "description": "Application pour le TP CI-CD",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "cross-env NODE_ENV=test jest --runInBand --forceExit",
    "lint": "node --check src/app.js && node --check tests/unit.test.js && node --check tests/integration.test.js && node --check tests/e2e.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
